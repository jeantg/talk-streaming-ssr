datasource db {
  provider = "sqlite"
  url      = "file:./dev.db"
}

model Comment {
  id       Int    @id @default(autoincrement())
  userName String
  srcImage String
  comment  String
  date     String
}

generator client {
  provider = "prisma-client-js"
}
